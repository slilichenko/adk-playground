# Processing images located on a Cloud Storage bucket using subagents

This is a variation on how images can be processed by subagents rather than by the root agent ([details](../image-processing-using-file-references)). 

This is what's happening here:

```mermaid
sequenceDiagram

```